# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / tooling
.vscode/
.idea/
*.swp

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json

# Node — not used by the site itself, but keeps things clean if you add tooling
node_modules/
npm-debug.log*
.env
.env.local

# Vercel
.vercel
